---
//layout imports
import PageHeader from "@/components/blocks/PageHeader.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";

//components imports
import { Button } from "@/starwind/button";
import { Checkbox } from "@/starwind/checkbox";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/starwind/select";
// import { Slider } from "@/starwind/slider";
import { Grid, List, ShoppingCart, Heart, Search } from "@lucide/astro";

const brands = [
  { name: "Coaster Furniture", checked: false },
  { name: "Fusion Dot High Fashion", checked: false },
  { name: "Unique Furniture Restor", checked: true },
  { name: "Dream Furniture Flipping", checked: false },
  { name: "Young Repurposed", checked: false },
  { name: "Green DIY furniture", checked: false },
];

const discountOffers = [
  { label: "20% Cashback", checked: false },
  { label: "5% Cashback Offer", checked: false },
  { label: "25% Discount Offer", checked: false },
];

const ratings = [
  { stars: 4, count: 2341, checked: false },
  { stars: 3, count: 1726, checked: false },
  { stars: 2, count: 258, checked: false },
  { stars: 1, count: 25, checked: false },
];

const categories = [
  { name: "Prestashop", checked: false },
  { name: "Magento", checked: false },
  { name: "Bigcommerce", checked: true },
  { name: "osCommerce", checked: false },
  { name: "3dcart", checked: false },
  { name: "Bags", checked: false },
  { name: "Accessories", checked: false },
  { name: "Jewellery", checked: false },
  { name: "Watches", checked: false },
];

const priceFilters = [
  { range: "$0.00 - $150.00", checked: false },
  { range: "$150.00 - $350.00", checked: true },
  { range: "$150.00 - $504.00", checked: false },
  { range: "$450.00 +", checked: false },
];

const colors = [
  { name: "Red", color: "bg-red-500" },
  { name: "Orange", color: "bg-orange-500" },
  { name: "Brown", color: "bg-amber-700" },
  { name: "Green", color: "bg-green-500" },
  { name: "Purple", color: "bg-purple-500" },
  { name: "Sky", color: "bg-sky-500" },
];

const products = [
  {
    id: 1,
    name: "Dictum morbi",
    originalPrice: "$52.00",
    salePrice: "$26.00",
    rating: 5,
    image: "/placeholder.svg?height=200&width=200",
    description:
      "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Magna in est adipiscing in phasellus non in justo.",
  },
  {
    id: 2,
    name: "Sodales sit",
    originalPrice: "$52.00",
    salePrice: "$26.00",
    rating: 5,
    image: "/placeholder.svg?height=200&width=200",
    description:
      "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Magna in est adipiscing in phasellus non in justo.",
  },
  {
    id: 3,
    name: "Nibh varius",
    originalPrice: "$52.00",
    salePrice: "$26.00",
    rating: 5,
    image: "/placeholder.svg?height=200&width=200",
    description:
      "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Magna in est adipiscing in phasellus non in justo.",
  },
  {
    id: 4,
    name: "Mauris quis",
    originalPrice: "$52.00",
    salePrice: "$26.00",
    rating: 5,
    image: "/placeholder.svg?height=200&width=200",
    description:
      "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Magna in est adipiscing in phasellus non in justo.",
  },
  {
    id: 5,
    name: "Morbi sagittis",
    originalPrice: "$52.00",
    salePrice: "$26.00",
    rating: 5,
    image: "/placeholder.svg?height=200&width=200",
    description:
      "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Magna in est adipiscing in phasellus non in justo.",
  },
  {
    id: 6,
    name: "Ultrices venenatis",
    originalPrice: "$52.00",
    salePrice: "$26.00",
    rating: 5,
    image: "/placeholder.svg?height=200&width=200",
    description:
      "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Magna in est adipiscing in phasellus non in justo.",
  },
  {
    id: 7,
    name: "Scelerisque dignissim",
    originalPrice: "$52.00",
    salePrice: "$26.00",
    rating: 5,
    image: "/placeholder.svg?height=200&width=200",
    description:
      "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Magna in est adipiscing in phasellus non in justo.",
  },
];

//   const renderStars = (rating: number) => {
//     return Array.from({ length: 5 }, (_, i) => (
//       <span key={i} class={`text-sm ${i < rating ? "text-yellow-400" : "text-gray-300"}`}>
//         ★
//       </span>
//     ))
//   }

// page header details
const pageHeader = {
  title: "Products Listing List Layout",
  caption: "List Layout",
  description: "",
};
---

<BaseLayout
  title="Product List"
  description="Browse our collection of fashion accessories and items."
>
  <PageHeader headerInfo={pageHeader} slot="pageHeader" />
  <div class="min-h-screen bg-white">
    {/* Header */}
    <div class="border-b border-gray-200 py-6 px-4">
      <div class="max-w-7xl mx-auto flex items-center justify-between">
        <div>
          <h1 class="text-2xl font-semibold text-blue-900 mb-1">
            Ecommerce Accesories & Fashion Item
          </h1>
          <p class="text-sm text-gray-500">
            About 9,620 results (0.62 seconds)
          </p>
        </div>

        <div class="flex items-center space-x-6">
          <div class="flex items-center space-x-2">
            <span class="text-sm text-gray-600">Per Page:</span>
            <Select defaultValue="12">
              <SelectTrigger class="w-16 h-8 text-sm">
                <SelectValue />
              </SelectTrigger>
              <SelectContent>
                <SelectItem value="12">12</SelectItem>
                <SelectItem value="24">24</SelectItem>
              </SelectContent>
            </Select>
          </div>

          <div class="flex items-center space-x-2">
            <span class="text-sm text-gray-600">Sort By:</span>
            <Select defaultValue="best-match">
              <SelectTrigger class="w-32 h-8 text-sm">
                <SelectValue />
              </SelectTrigger>
              <SelectContent>
                <SelectItem value="best-match">Best Match</SelectItem>
                <SelectItem value="price-low">Price: Low to High</SelectItem>
              </SelectContent>
            </Select>
          </div>

          <div class="flex items-center space-x-2">
            <span class="text-sm text-gray-600">View:</span>
            <div class="flex border border-gray-300 rounded">
              <Button
                variant="ghost"
                size="sm"
                class="h-8 w-8 p-0 bg-blue-500 text-white"
              >
                <Grid class="h-4 w-4" />
              </Button>
              <Button variant="ghost" size="sm" class="h-8 w-8 p-0">
                <List class="h-4 w-4" />
              </Button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="max-w-7xl mx-auto px-4 py-8">
      <div class="grid grid-cols-12 gap-8">
        {/* Sidebar */}
        <div class="col-span-3 space-y-8">
          {/* Product Brand */}
          <div>
            <h3 class="text-lg font-semibold text-blue-900 mb-4">
              Product Brand
            </h3>
            <div class="space-y-3">
              {
                brands.map((brand, index) => (
                  <div key={index} class="flex items-center space-x-2">
                    <Checkbox
                      id={`brand-${index}`}
                      checked={brand.checked}
                      label={brand.name}
                    />
                  </div>
                ))
              }
            </div>
          </div>

          {/* Discount Offer */}
          <div>
            <h3 class="text-lg font-semibold text-blue-900 mb-4">
              Discount Offer
            </h3>
            <div class="space-y-3">
              {
                discountOffers.map((offer, index) => (
                  <div key={index} class="flex items-center space-x-2">
                    <Checkbox
                      id={`offer-${index}`}
                      checked={offer.checked}
                      label={offer.label}
                    />
                  </div>
                ))
              }
            </div>
          </div>

          {/* Rating Item */}
          <div>
            <h3 class="text-lg font-semibold text-blue-900 mb-4">
              Rating Item
            </h3>
            <div class="space-y-3">
              {
                ratings.map((rating, index) => (
                  <div key={index} class="flex items-center space-x-2">
                    <Checkbox id={`rating-${index}`} checked={rating.checked} />
                    <div class="flex items-center space-x-2">
                      {/* <div class="flex">{renderStars(rating.stars)}</div> */}
                      <span class="text-sm text-gray-500">
                        ({rating.count})
                      </span>
                    </div>
                  </div>
                ))
              }
            </div>
          </div>

          {/* Categories */}
          <div>
            <h3 class="text-lg font-semibold text-blue-900 mb-4">Categories</h3>
            <div class="space-y-3">
              {
                categories.map((category, index) => (
                  <div key={index} class="flex items-center space-x-2">
                    <Checkbox
                      id={`category-${index}`}
                      checked={category.checked}
                      label={category.name}
                    />
                  </div>
                ))
              }
            </div>
          </div>

          {/* Price Filter */}
          <div>
            <h3 class="text-lg font-semibold text-blue-900 mb-4">
              Price Filter
            </h3>
            <div class="space-y-4">
              <div class="space-y-3">
                {
                  priceFilters.map((filter, index) => (
                    <div key={index} class="flex items-center space-x-2">
                      <Checkbox
                        id={`price-${index}`}
                        checked={filter.checked}
                        label={filter.range}
                      />
                    </div>
                  ))
                }
              </div>
              <!-- <div class="pt-4">
              <Slider
                value={priceRange}
                onValueChange={setPriceRange}
                max={500}
                step={10}
                class="w-full"
              />
              <div class="flex justify-between text-sm text-gray-500 mt-2">
                <span>${priceRange[0]}</span>
                <span>${priceRange[1]}</span>
              </div>
            </div> -->
            </div>
          </div>

          {/* Filter By Color */}
          <div>
            <h3 class="text-lg font-semibold text-blue-900 mb-4">
              Filter By Color
            </h3>
            <div class="flex flex-wrap gap-3">
              {
                colors.map((color, index) => (
                  <div key={index} class="flex items-center space-x-2">
                    <div
                      class={`w-4 h-4 rounded-full ${color.color} cursor-pointer border border-gray-300`}
                    />
                    <span class="text-sm text-gray-700">{color.name}</span>
                  </div>
                ))
              }
            </div>
          </div>
        </div>

        {/* Product Grid */}
        <div class="col-span-9">
          <div class="space-y-6">
            {
              products.map((product) => (
                <div
                  key={product.id}
                  class="flex bg-white rounded-lg shadow-sm border border-gray-200 p-6"
                >
                  <div class="w-48 h-48 bg-gray-100 rounded-lg flex items-center justify-center mr-6">
                    <img
                      src={product.image || "/placeholder.svg"}
                      alt={product.name}
                      class="w-full h-full object-contain"
                    />
                  </div>

                  <div class="flex-1 space-y-3">
                    <div class="flex items-start justify-between">
                      <div>
                        <h3 class="text-lg font-semibold text-blue-900 mb-1">
                          {product.name}
                        </h3>
                        <div class="flex items-center space-x-2 mb-2">
                          {/* <div class="flex">{renderStars(product.rating)}</div> */}
                          <div class="flex space-x-1">
                            <div class="w-2 h-2 bg-orange-400 rounded-full" />
                            <div class="w-2 h-2 bg-pink-400 rounded-full" />
                            <div class="w-2 h-2 bg-blue-400 rounded-full" />
                          </div>
                        </div>
                        <div class="flex items-center space-x-3 mb-3">
                          <span class="text-lg font-semibold text-blue-900">
                            {product.salePrice}
                          </span>
                          <span class="text-lg text-red-500 line-through">
                            {product.originalPrice}
                          </span>
                        </div>
                      </div>

                      <div class="flex space-x-2">
                        <Button variant="ghost" size="sm" class="p-2">
                          <ShoppingCart class="h-4 w-4 text-blue-900" />
                        </Button>
                        <Button variant="ghost" size="sm" class="p-2">
                          <Heart class="h-4 w-4 text-blue-900" />
                        </Button>
                        <Button variant="ghost" size="sm" class="p-2">
                          <Search class="h-4 w-4 text-blue-900" />
                        </Button>
                      </div>
                    </div>

                    <p class="text-sm text-gray-600 leading-relaxed">
                      {product.description}
                    </p>
                  </div>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>
